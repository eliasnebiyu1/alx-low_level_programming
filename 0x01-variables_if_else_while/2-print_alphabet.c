#include<stdio.h>
/**
 * main - Responsible for starting the execution and termination
 *
 * Return: returns 0
 */
int main(void)
{
char lca;
lca = 'a';

while(lca <= 'z')
{
putchar(lca);
lca++;
}
return (0);
}

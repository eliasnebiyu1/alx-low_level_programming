#include "main.h"
/**
 * int_abs - prints absolute value of numbers 
 *
 * Return: always returns positive
 */

int _abs(int i)
{

if (i >= 0)
{
return (i);
}

else
{
return (-i);
}
_putchar('\n');
}

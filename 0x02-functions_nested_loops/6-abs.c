#include <stdio.h>
#include <stdlib.h>
/**
 * int abs - prints absolute value of numbers 
 *
 * Return: re
 */

int _abs(int)
{
int i;

if (i >= 0)
{
return (i);
}

else
{
return (-i);
}
_putchar('\n');
}

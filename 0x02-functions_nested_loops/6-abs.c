#include "main.h"
/**
 * _abs - prints absolute value of numbers
 *
 * @i: stores values
 *
 * Return: always returns positive
 */

int _abs(int i)
{

if (i >= 0)
{
return (i);
}

else
{
return (-i);
}
_putchar('\n');
}
